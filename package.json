{
  "name": "automata-consumer",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index.js",
    "build": "npm run clean && tsc",
    "clean": "rm -rfv dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@delbio/automata": "^1.1.1",
    "commander": "^2.12.2"
  },
  "devDependencies": {
    "@types/node": "^8.0.54"
  }
}
